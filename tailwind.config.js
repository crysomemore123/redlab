/* src/components/ContactSection.module.css */

/*
  This file can define CSS Custom Properties (variables) if you want
  to share specific design tokens across your project or reference them
  in your tailwind.config.js.

  If your ContactSection.tsx is styled entirely with Tailwind utility classes,
  specific class definitions (like .formInput, .contactTitle) are no longer needed here
  unless you have highly specific styles not achievable with Tailwind.
*/

:root {
  /*
    Example: If you preferred to define your brand red here
    and have tailwind.config.js reference it:
    --color-brand-red: #D22B2B;
    --color-brand-red-darker: #b91c1c;

    And your font stacks:
    --font-family-oswald: 'Oswald', ui-sans-serif, system-ui, sans-serif;
    --font-family-roboto: 'Roboto', ui-sans-serif, system-ui, sans-serif;
  */

  /*
    If your tailwind.config.js defines these values directly in its theme.extend,
    you might not need to repeat them here. This file then becomes optional
    or can be used for very specific, non-Tailwind overrides if any.
  */
}

/*
  Remove old class definitions like .formInput, .submitButton, etc.,
  if they are no longer used by your .tsx component.
*/